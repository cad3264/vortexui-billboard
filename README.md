# VortexUI
An alternate framework for rendering UI. Currently only supports rendering text in 3D space, but will have many features soon!
**Current Version**: v1.0.1

Modified by @cad4 to add billboarding. You *could* use the VortexFont without it if you want rotation
